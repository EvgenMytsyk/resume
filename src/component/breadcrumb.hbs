{{!-- Секція хлібні крошки --}}
<div class="mt-5">
    <ol class="breadcrumb">
        {{#each breadcrumb}}
        <li class="breadcrumb-item">
            <a href="{{url}}">{{name}}</a>
        </li>
        {{/each}}
    </ol>
</div>